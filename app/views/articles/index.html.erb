<%= render "navbar" %>

<% @articles.each do |article| %>
  <div id="articles">
    <strong><%= link_to article.title, article %></strong>
    <p><%= "#{article.content[0..150]}..." %></p>
  </div>
<% end %>
